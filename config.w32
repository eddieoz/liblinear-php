// $Id$
// vim:ft=javascript

// If your extension references something external, use ARG_WITH
// ARG_WITH("liblinear", "for liblinear support", "no");

// Otherwise, use ARG_ENABLE
// ARG_ENABLE("liblinear", "enable liblinear support", "no");

if (PHP_LIBLINEAR != "no") {
	EXTENSION("liblinear", "liblinear.c");
}

